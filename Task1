#include <stdio.h>

main()
{
  int i,c,m;
  printf("Side of square: ");
  scanf("%d", &c);
  if (c>0 && c<80)
  {
  for (m=1; m<=c; m++){
  for (i=1; i<=c; i++){
  printf("* ");}
  printf("\n");
  }
  }

  else
    printf("Number has to be from 1 to 80");
    }
