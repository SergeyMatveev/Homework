#include <stdio.h>
#include <math.h>

void main()
{
    int input = 0;
    int i = 0;
    int result = 0;

    scanf("%i", &input);

    while (input > 0)
    {
        result += (input % 10)*pow(2, i);
        input = input / 10;
        i++;
    }
    printf("%i\n\n", result);

    return 0;
}
